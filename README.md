# Git Workflow Midterm Lab

This project is a sample application created for the Midterm Laboratory Examination.
It demonstrates a professional Git workflow using feature branches, commits, merges,
merge conflict resolution, and GitHub Pull Requests.

## Project Structure
- `index.html` – main HTML entry point
- `style.css` – stylesheet for the project
- `script.js` – client-side JavaScript /modified
